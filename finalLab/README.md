# Книжный Магазин - Система управления

Индивидуальный проект по разработке системы управления книжным магазином с использованием Spring Boot, REST API и Spring Security.

## 📋 Описание проекта

Система управления книжным магазином, включающая:
- CRUD операции для всех сущностей через REST API
- Систему авторизации и регистрации с разделением прав доступа
- 3 роли пользователей: ADMIN, MANAGER, USER
- Валидацию данных на всех уровнях
- Swagger документацию для API
- Современный UI с Bootstrap

## 🗄️ Структура базы данных

Проект использует PostgreSQL базу данных со следующими таблицами:

1. **users** - Пользователи системы
2. **roles** - Роли пользователей
3. **authors** - Авторы книг
4. **publishers** - Издательства
5. **genres** - Жанры книг
6. **books** - Книги
7. **orders** - Заказы
8. **order_items** - Позиции заказа
9. **reviews** - Отзывы на книги
10. **warehouse** - Склад (остатки книг)

## 🚀 Установка и запуск

### Требования
- Java 17 или выше
- Maven 3.6+
- PostgreSQL 12+

### Настройка базы данных

1. Создайте базу данных:
```bash
psql -U ekononykhin -c "CREATE DATABASE bookstore_db;"
```

Или используйте скрипт:
```bash
psql -U ekononykhin -f create_database.sql
```

2. Настройте `application.properties`:
```properties
spring.datasource.url=jdbc:postgresql://localhost:5432/bookstore_db
spring.datasource.username=ekononykhin
spring.datasource.password=1111
```

### Запуск приложения

```bash
cd finalLab
mvn spring-boot:run
```

Приложение будет доступно на: http://localhost:8083

## 👥 Тестовые пользователи

После первого запуска создаются следующие пользователи:

| Роль | Имя пользователя | Пароль | Описание |
|------|------------------|--------|----------|
| ADMIN | admin | Admin123! | Полный доступ ко всем функциям |
| MANAGER | manager | Manager123! | Управление заказами и складом |
| USER | user | User123! | Просмотр каталога и создание заказов |

## 🌐 API Endpoints

### Книги
- `GET /api/books` - получить все книги
- `GET /api/books/{id}` - получить книгу по ID
- `GET /api/books/search?title={title}` - поиск книг
- `POST /api/books` - создать книгу
- `PUT /api/books/{id}` - обновить книгу
- `DELETE /api/books/{id}` - удалить книгу

### Авторы
- `GET /api/authors` - получить всех авторов
- `GET /api/authors/{id}` - получить автора по ID
- `POST /api/authors` - создать автора
- `PUT /api/authors/{id}` - обновить автора
- `DELETE /api/authors/{id}` - удалить автора

### Заказы
- `GET /api/orders` - получить все заказы
- `GET /api/orders/{id}` - получить заказ по ID
- `GET /api/orders/user/{userId}` - заказы пользователя
- `POST /api/orders` - создать заказ
- `PUT /api/orders/{id}/status?status={status}` - обновить статус заказа
- `DELETE /api/orders/{id}` - удалить заказ

Полная документация API доступна в Swagger UI: http://localhost:8083/swagger-ui.html

## 🔐 Права доступа

### ADMIN
- Полный доступ ко всем функциям
- Управление книгами, авторами, издательствами, жанрами
- Просмотр всех заказов
- Управление пользователями

### MANAGER
- Управление заказами
- Управление складом
- Просмотр статистики

### USER
- Просмотр каталога книг
- Создание заказов
- Просмотр своих заказов
- Оставление отзывов

## 📁 Структура проекта

```
finalLab/
├── src/
│   ├── main/
│   │   ├── java/com/bookstore/
│   │   │   ├── config/          # Конфигурация (Security, Swagger, DataInitializer)
│   │   │   ├── controller/
│   │   │   │   ├── api/         # REST API контроллеры
│   │   │   │   └── web/         # Web контроллеры (UI)
│   │   │   ├── dto/             # Data Transfer Objects
│   │   │   ├── exception/       # Обработка ошибок
│   │   │   ├── model/           # Сущности (Entity)
│   │   │   ├── repository/      # Репозитории (JPA)
│   │   │   ├── security/        # Spring Security
│   │   │   └── service/         # Бизнес-логика
│   │   └── resources/
│   │       ├── static/css/      # CSS стили
│   │       └── templates/      # Thymeleaf шаблоны
│   └── test/
├── pom.xml
├── create_database.sql
└── README.md
```

## 🛠️ Технологии

- **Spring Boot 3.2.0** - основной фреймворк
- **Spring Security** - авторизация и аутентификация
- **Spring Data JPA** - работа с БД
- **PostgreSQL** - база данных
- **Thymeleaf** - шаблонизатор
- **Bootstrap 5** - UI фреймворк
- **Swagger/OpenAPI** - документация API
- **Bean Validation** - валидация данных
- **BCrypt** - шифрование паролей

## 📝 Особенности реализации

1. **Валидация данных**: Использование аннотаций `@Valid`, `@NotNull`, `@NotBlank`, `@Size`, `@Email` и др.
2. **Обработка ошибок**: Глобальный обработчик `@RestControllerAdvice` для единообразных ответов об ошибках
3. **Безопасность**: Шифрование паролей с помощью BCrypt, защита от CSRF
4. **REST API**: Все операции с данными выполняются через REST API
5. **Разделение прав**: Разные пункты меню в header для разных ролей
6. **Документация**: Автоматическая генерация документации API через Swagger

## 📄 Лицензия

Проект создан в учебных целях.


